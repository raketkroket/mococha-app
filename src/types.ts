export type Category = { id: string; parent_id: string | null; slug: string; title: string; short_description: string | null; image_url: string | null; product_count: number; sort_order: number; is_info: boolean; is_featured: boolean; };
export type ProductImage = { id: string; product_id: string; url: string; alt: string | null; sort_order: number };
export type Product = { id: string; category_id: string | null; slug: string; title: string; subtitle: string | null; description: string | null; included: string | null; excluded: string | null; dimensions: string | null; styling_options: string | null; personalization_options: string | null; price: number; deposit: number; vat_rate: number; stock: number; requires_large_bus: boolean; pickup_only: boolean; delivery_only: boolean; personaliseerbaar: boolean; status: string; legacy_id: string | null; legacy_url: string | null; theme: string | null; color: string | null; sort_date: string; images?: ProductImage[]; };
export type Theme = { id: string; slug: string; title: string; description: string | null; image_url: string | null; colors: string | null; sort_order: number };
export type StylingPackage = { id: string; slug: string; title: string; description: string | null; price: number; tier: string; sort_order: number };
export type SurchargeRule = { id: string; code: string; label: string; amount: number; is_active: boolean };
export type CartLine = { id: string; product_id: string | null; title: string; image_url: string; unit_price: number; deposit: number; vat_rate: number; quantity: number; selected_options: Record<string, string>; event_date: string | null; requires_large_bus: boolean; group_label: string; };
export type Review = { id: string; name: string; rating: number; body: string; created_at: string };
export type OrderStatus = "Concept opgeslagen" | "Offerte aangevraagd" | "In beoordeling" | "Aanpassing nodig" | "Wacht op betaling" | "Bevestigd" | "Styling in voorbereiding" | "Klaar voor afhalen" | "Onderweg" | "Opgebouwd" | "Retour verwacht" | "Afgerond" | "Geannuleerd";
export const ORDER_STATUSES: OrderStatus[] = ["Concept opgeslagen","Offerte aangevraagd","In beoordeling","Aanpassing nodig","Wacht op betaling","Bevestigd","Styling in voorbereiding","Klaar voor afhalen","Onderweg","Opgebouwd","Retour verwacht","Afgerond","Geannuleerd"];
